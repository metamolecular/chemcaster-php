<?php

class Chemcaster_Query extends Chemcaster_Representation
{
    /*
    public $query;

    public $images;

    public $index;

    public $destroy;

    public function __construct( Chemcaster_Link $Link )
    {
        $index = json_decode( $Link->get() );

        $this->query = $index->query;

        $this->images = new Chemcaster_Link(
            $index->images->name,
            $index->images->uri,
            $index->images->media_type
        );

        $this->index = new Chemcaster_Link(
            $index->index->name,
            $index->index->uri,
            $index->index->media_type
        );

        $this->destroy = new Chemcaster_Link(
            $index->destroy->name,
            $index->destroy->uri,
            $index->destroy->media_type
        );
    }
    *
     */
}

?>
